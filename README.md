# Lucky Pulse Demo

This repo contains a single-file, client-side gambling-style demo in `index.html`.

## Run locally

```bash
python -m http.server 8000
```

Then open `http://localhost:8000/index.html`.
